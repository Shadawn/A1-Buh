#Если НЕ Клиент Тогда
	Функция ОбработкаПроверкиЗаполненияПеред(Объект, Отказ, ПроверяемыеРеквизиты) Экспорт
		Если НЕ ЗначениеЗаполнено(Объект.ОрганизацияПолучатель) И ЗначениеЗаполнено(Объект.Организация) Тогда
			Объект.ОрганизацияПолучатель = Объект.Организация; //По умолчанию организация-получатель это организация.
		КонецЕсли;
	КонецФункции 
#КонецЕсли
 