
&Вместо("ПолучитьКонтекстЭДО")
Процедура А1Бух_ПолучитьКонтекстЭДО(ВыполняемоеОповещение, ОбновитьСейчас, ЭтоОбновлениеИзМастера)
	//Не критично для разработчиков и может падать с исключениями - убираем. 
	Если ОбщегоНазначенияКлиентСервер.РежимОтладки() Тогда Возврат; КонецЕсли; 
	
	ПродолжитьВызов(ВыполняемоеОповещение, ОбновитьСейчас, ЭтоОбновлениеИзМастера);
КонецПроцедуры
