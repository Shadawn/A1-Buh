
&Вместо("ПлатежГосОргану")
Функция А1Бух_ПлатежГосОргану(ВидОперации, Контрагент)
	Результат = ПродолжитьВызов(ВидОперации, Контрагент);
	//Иногда при битых ссылках возвращает Неопределено, что не проходит булевые операции.
	Если Результат = Неопределено Тогда
		Результат = Ложь;
	КонецЕсли;
	Возврат Результат;
КонецФункции
